<form method="post" action="{{ path('app_payment_info_delete', {'licensePlate': payment_info.licensePlate}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_info.licensePlate) }}">
    <button class="btn">Delete</button>
</form>
